---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CommunityNodes from '../../components/CommunityNodes.astro';
import LearningPath from '../../components/LearningPath.astro';
---

<BaseLayout title="Community | Dax Kenji" description="Dax Kenji's active participation in local and global software development, Web3, and Artificial Intelligence communities.">
  <main class="community-page">
    <div class="container">
      <header class="community-header">
        <h1 class="page-title">// community_engagement</h1>
        <p class="subtitle">
          Events, talks, and meetups in Bolivia. AI and Web3 from the inside, not from the textbook.
        </p>
      </header>
    </div>
    
    <CommunityNodes />
    <LearningPath />
  </main>
</BaseLayout>

<style>
  .community-page {
    padding-top: 4rem;
    background-color: var(--bg-primary);
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .community-header {
    margin-bottom: 4rem;
  }

  .page-title {
    font-family: var(--font-mono);
    font-size: clamp(2rem, 4vw, 3rem);
    color: var(--hash-color);
    margin-bottom: 1.5rem;
  }

  .subtitle {
    font-family: var(--font-body);
    font-size: 1.25rem;
    color: var(--ink-secondary);
    line-height: 1.6;
    max-width: 800px;
  }

  @media (max-width: 768px) {
    .community-page {
      padding-top: 2rem;
    }
    
    .community-header {
      margin-bottom: 2rem;
    }
  }
</style>

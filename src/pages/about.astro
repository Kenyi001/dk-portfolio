---
import BaseLayout from '../layouts/BaseLayout.astro';
import SkillsSection from '../components/SkillsSection.astro';
import ExperienceSection from '../components/ExperienceSection.astro';
import EducationSection from '../components/EducationSection.astro';
import AchievementsSection from '../components/AchievementsSection.astro';
import CommunityNodes from '../components/CommunityNodes.astro';
---

<BaseLayout title="About | Dax Kenji">
  <div class="about-page">
    <div class="container">
      <header class="about-header">
        <span class="tagline">Sobre mí</span>
        <h1 class="page-title">AI Backend Engineer con base en producción real.</h1>
        <div class="bio-extended">
          <p>
            Ingeniero de sistemas en Santa Cruz, Bolivia. Construyo sistemas backend con IA en Node.js, TypeScript y C#, con foco en productos que funcionan en producción real y son claros para equipos técnicos y de negocio.
          </p>
          <p>
            Mi trabajo más reciente incluye sistemas RAG, OCR, Redis, Webhooks y evaluación cloud para flujos bancarios. En paralelo construyo proyectos Web3 y productos SaaS, con experiencia en hackathons internacionales y ranking #89 mundial en DEV3PACK.
          </p>
        </div>
      </header>
    </div>
    
    <SkillsSection />
    <ExperienceSection />
    <EducationSection />
    <AchievementsSection />
    <CommunityNodes />
    
    <div class="contact-cta">
      <div class="container text-center">
        <h2>¿Buscás un perfil AI Backend?</h2>
        <p>Estoy abierto a equipos remotos internacionales y oportunidades presenciales seleccionadas en Bolivia.</p>
        <a href="/contact" class="cta-link">Contactar</a>
      </div>
    </div>
  </div>
</BaseLayout>

<style>
  .about-page {
    padding-top: 4rem;
    background-color: var(--bg-primary);
  }

  .container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .text-center {
    text-align: center;
  }

  .about-header {
    margin-bottom: 6rem;
  }

  .tagline {
    font-family: var(--font-mono);
    color: var(--accent-2);
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    display: block;
    margin-bottom: 1rem;
  }

  .page-title {
    font-family: var(--font-display);
    font-size: 4.6rem;
    color: var(--ink-primary);
    line-height: 1;
    margin-bottom: 3rem;
  }

  .bio-extended p {
    font-family: var(--font-body);
    font-size: 1.25rem;
    color: var(--ink-primary);
    line-height: 1.8;
    margin-bottom: 1.5rem;
  }

  .contact-cta {
    padding: 6rem 2rem;
    background-color: var(--bg-secondary);
    border-top: 1px solid var(--border-subtle);
  }

  .contact-cta h2 {
    font-family: var(--font-display);
    font-size: 2.25rem;
    color: var(--ink-primary);
    margin-bottom: 1rem;
  }

  .contact-cta p {
    font-family: var(--font-body);
    color: var(--ink-secondary);
    font-size: 1.1rem;
    margin-bottom: 2.5rem;
  }

  .cta-link {
    display: inline-block;
    font-family: var(--font-mono);
    color: var(--bg-primary);
    background-color: var(--accent);
    padding: 0.8rem 1.6rem;
    border-radius: 6px;
    font-weight: bold;
    text-decoration: none;
    transition: all 0.3s ease;
    border: 1px solid var(--accent);
  }

  .cta-link:hover {
    background-color: transparent;
    color: var(--accent);
  }

  @media (max-width: 768px) {
    .about-page {
      padding-top: 2rem;
    }
    
    .about-header {
      margin-bottom: 3rem;
    }
    
    .bio-extended p {
      font-size: 1.1rem;
    }

    .page-title {
      font-size: 2.9rem;
    }

    .contact-cta {
      padding: 4rem 1.5rem;
    }
  }
</style>
